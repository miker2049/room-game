/**
 * A section is a place where entities can be together and interact.
 * Sections connect to each other on a simple csv grid and are identified by a coordinate on that grid, as defined in world.ts
 */
